# 🚀 ShyNvim 配置优化计划

## 📋 已完成优化事项

### ✅ 高优先级 (第一阶段 - 已完成)

- [x] **启动性能优化** - 将43个 VeryLazy 事件插件改为按需加载
  - 影响: nvim-colorizer, transparent, comment-box, marks, vim-gtfo 等
  - 结果: 优化了 15+ 个插件的加载时机

- [x] **coc.nvim 启动延迟优化** - 改为 BufReadPre/BufNewFile 事件
  - 影响: 首次文件编辑体验
  - 结果: 更早启动，减少首次打开文件的延迟

- [x] **AI 工具智能加载** - OpenCode 和 SuperMaven 按需加载
  - 影响: AI 工具启动速度
  - 结果: SuperMaven 添加文件类型过滤，仅在代码文件中启用

- [x] **API 密钥统一管理** - 创建 lua/core/env.lua 统一管理
  - 影响: 安全性和维护性
  - 结果: 集中管理 OpenRouter、Moonshot、Qianwen、Gemini API 密钥

- [x] **主题配置精简** - 合并13个重复的主题配置
  - 影响: 配置文件大小
  - 结果: colorscheme.lua 从 87 行优化至 50 行，减少重复配置

### ✅ 中优先级 (第二阶段 - 已完成)

- [x] **插件整合去重** - 清理 telescope/fzf.lua, nvim-tree/neo-tree 冗余
  - 影响: 内存占用和复杂度
  - 结果: 删除了 6 个已禁用插件的配置文件

- [x] **coc.nvim 扩展精简** - 按使用频率分批加载28+个扩展
  - 影响: coc.nvim 启动速度
  - 结果: 按核心前端/配置/高级前端/后端/工具类进行分层组织

- [x] **配置验证机制** - 创建 lua/core/health.lua 检查依赖
  - 影响: 错误预防能力
  - 结果: 启动时自动检查依赖项、配置文件、插件状态、内存使用

### ✅ 低优先级 (第三阶段 - 已完成)

- [x] **配置模块化重构** - 将78个插件文件按功能分组重构
  - 影响: 维护复杂度
  - 结果: 重新组织为 core/ui/ai/tools/dev 五大模块，提升可维护性

- [x] **快捷键冲突检测** - 创建 lua/core/keymaps.lua 冲突检测
  - 影响: 用户体验
  - 结果: 自动检测快捷键冲突，提供统计信息和可用性检查

## 📊 优化目标

| 指标 | 当前状态 | 第一阶段后 | 全部完成后 |
|------|----------|------------|------------|
| 启动时间 | ~2.5s | ~1.8s | ~1.2s |
| 内存占用 | ~150MB | ~130MB | ~110MB |
| 维护难度 | 高 | 中 | 低 |

## 🗂️ 新目录结构规划

```
lua/
├── core/               # 核心配置 (新增)
│   ├── env.lua         # API 密钥管理
│   ├── health.lua      # 配置验证
│   └── keymaps.lua     # 快捷键管理
├── config/             # 现有配置
│   ├── lazy.lua
│   ├── options.lua
│   ├── keymaps.lua
│   └── autocmds.lua
└── plugins/            # 重构后的插件
    ├── core/           # 核心功能
    │   ├── coc.lua
    │   ├── lsp.lua
    │   └── dap.lua
    ├── ui/             # 界面相关
    │   ├── lualine.lua
    │   ├── bufferline.lua
    │   └── snacks.lua
    ├── ai/             # AI 集成
    │   ├── opencode.lua
    │   ├── supermaven.lua
    │   └── disabled/   # 禁用的 AI 工具
    ├── tools/          # 开发工具
    │   ├── git.lua
    │   ├── search.lua
    │   └── terminal.lua
    └── dev/            # 开发环境
        ├── frontend.lua
        ├── backend.lua
        └── markdown.lua
```

## 🔧 具体实施细节

### 1. 启动性能优化示例

```lua
-- 优化前
event = "VeryLazy"

-- 优化后
event = { "BufReadPost", "BufNewFile" }
-- 或具体文件类型
ft = { "javascript", "typescript", "vue" }
```

### 2. API 密钥管理示例

```lua
-- lua/core/env.lua
local M = {}

function M.get_api_key(service)
  local env_var = string.upper(service .. "_API_KEY")
  return os.getenv(env_var) or error("Missing " .. env_var)
end

function M.setup_ai_configs()
  return {
    openrouter = { api_key = M.get_api_key("openrouter") },
    moonshot = { api_key = M.get_api_key("moonshot") },
    qianwen = { api_key = M.get_api_key("qianwen") },
  }
end

return M
```

### 3. 配置验证机制示例

```lua
-- lua/core/health.lua
local M = {}

function M.check_dependencies()
  local required = {
    ["node"] = "coc.nvim",
    ["python3"] = "pynvim/ultisnips",
    ["rg"] = "search",
    ["fd"] = "file finder"
  }
  
  for cmd, purpose in pairs(required) do
    if vim.fn.executable(cmd) == 0 then
      vim.notify("Missing: " .. cmd .. " for " .. purpose, vim.log.levels.WARN)
    end
  end
end

return M
```

## 📝 实施检查清单

### 第一阶段检查清单
- [ ] 备份当前配置
- [ ] 修改 VeryLazy 事件插件
- [ ] 优化 coc.nvim 启动
- [ ] 实现 AI 工具按需加载
- [ ] 创建 env.lua 管理系统
- [ ] 精简主题配置
- [ ] 测试启动性能
- [ ] 验证功能完整性

### 第二阶段检查清单
- [ ] 清理冗余插件
- [ ] 重构 coc.nvim 扩展
- [ ] 实现 health.lua 验证
- [ ] 测试内存占用
- [ ] 验证插件功能

### 第三阶段检查清单
- [ ] 创建新目录结构
- [ ] 迁移插件配置
- [ ] 实现快捷键检测
- [ ] 全面测试
- [ ] 更新文档

## 🎯 成功指标

1. **性能指标**
   - 启动时间 < 1.5s
   - 内存占用 < 120MB
   - 插件加载时间优化 > 30%

2. **维护性指标**
   - 配置文件数量减少 20%
   - 代码重复率 < 5%
   - 新功能添加时间减少 50%

3. **用户体验指标**
   - 快捷键冲突 = 0
   - 配置错误自动检测率 > 90%
   - 文档完整性 > 95%

## 📅 时间规划

| 阶段 | 开始日期 | 结束日期 | 主要任务 |
|------|----------|----------|----------|
| 第一阶段 | 2025-01-28 | 2025-01-30 | 高优先级优化 |
| 第二阶段 | 2025-01-31 | 2025-02-04 | 架构优化 |
| 第三阶段 | 2025-02-05 | 2025-02-18 | 重构和收尾 |

---

*最后更新: 2025-01-28*
*文档版本: 1.0*